{
  "name": "rate-limiting-example",
  "private": true,
  "version": "0.1.0",
  "description": "Real proof of express-rate-limit — hammering a real endpoint past its real threshold and getting real 429 responses back.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "demo": "node demo.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^5.2.1",
    "express-rate-limit": "^8.5.2"
  }
}
