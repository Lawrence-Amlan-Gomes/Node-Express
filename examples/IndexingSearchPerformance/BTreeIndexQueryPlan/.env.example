# Real .env files (git-ignored by this project's root .gitignore) hold
# secrets and per-environment values — never commit a real one.
#
# Its OWN dedicated schema, per this project's standing rule: any mini-project
# that creates/drops real indexes or columns via raw SQL needs full, isolated
# ownership of its schema, the same reasoning as every other "db push"-using
# mini-project in this codebase. See build-conventions.md.
DATABASE_URL="postgres://user:password@host:5432/postgres?schema=node_express_learning_indexing_btree"
