{
  "name": "testing-error-cases",
  "private": true,
  "version": "0.1.0",
  "description": "Real Supertest tests proving validation middleware really rejects bad input, and that the error-handling middleware really catches a thrown error — not just the happy path.",
  "type": "module",
  "scripts": {
    "test": "node --no-warnings --experimental-vm-modules node_modules/.bin/jest --reporters=default --verbose"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^5.2.1"
  },
  "devDependencies": {
    "jest": "^30.4.1",
    "supertest": "^7.1.4"
  }
}
