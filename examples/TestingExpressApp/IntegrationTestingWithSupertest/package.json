{
  "name": "integration-testing-with-supertest",
  "private": true,
  "version": "0.1.0",
  "description": "Real Supertest integration tests against a real Express app — supertest talks to the app's real route handlers directly, without needing app.listen() on a real port at all.",
  "type": "module",
  "scripts": {
    "test": "node --no-warnings --experimental-vm-modules node_modules/.bin/jest --reporters=default --verbose"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^5.2.1"
  },
  "devDependencies": {
    "jest": "^30.4.1",
    "supertest": "^7.1.4"
  }
}
