{
  "name": "routes-params-query-body-example",
  "private": true,
  "version": "0.1.0",
  "description": "Real, runnable Express routes: params, query, a real parsed JSON body, and a real router split into its own separate file.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "demo": "node demo.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}
