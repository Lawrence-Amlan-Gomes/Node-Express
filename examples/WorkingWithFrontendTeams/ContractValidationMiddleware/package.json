{
  "name": "contract-validation-middleware",
  "private": true,
  "version": "0.1.0",
  "description": "A real OpenAPI spec wired in as real, enforced request-validation middleware — a request that violates the spec never reaches the controller at all.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "demo": "node demo.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^5.2.1",
    "express-openapi-validator": "^5.6.2"
  }
}
