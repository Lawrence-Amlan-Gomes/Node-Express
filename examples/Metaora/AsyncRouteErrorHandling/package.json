{
  "name": "metaora-async-route-error-handling",
  "private": true,
  "version": "0.1.0",
  "description": "Real, runnable proof of Metaora's own interview snippet: an async Express route wrapped in try/catch, forwarding real errors to a real central error-handling middleware via next(err).",
  "type": "commonjs",
  "main": "server.js",
  "scripts": {
    "db:push": "prisma db push",
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@prisma/client": "^6.19.0",
    "dotenv": "^17.4.2",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "prisma": "^6.19.0"
  }
}
