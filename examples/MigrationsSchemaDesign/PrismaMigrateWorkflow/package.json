{
  "name": "prisma-migrate-workflow-example",
  "private": true,
  "version": "0.1.0",
  "description": "Real, runnable proof of Prisma Migrate's versioned schema evolution against a real remote PostgreSQL database — two real, applied migrations, not a single one-shot db push.",
  "type": "commonjs",
  "main": "demo.js",
  "scripts": {
    "start": "node server.js",
    "demo": "node demo.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@prisma/client": "^6.19.0",
    "dotenv": "^17.4.2",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "prisma": "^6.19.0"
  }
}
