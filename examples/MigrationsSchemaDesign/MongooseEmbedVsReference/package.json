{
  "name": "mongoose-embed-vs-reference-example",
  "private": true,
  "version": "0.1.0",
  "description": "Real, runnable proof of MongoDB's two real schema design options — embedding related data in one document vs referencing it across separate collections — against a real remote MongoDB Atlas database.",
  "type": "commonjs",
  "main": "demo.js",
  "scripts": {
    "demo": "node demo.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "mongoose": "^8.20.1"
  }
}
