# Real .env files (git-ignored by this project's root .gitignore) hold
# secrets and per-environment values — never commit a real one.
#
# Same real "express" database as the previous topic, but three NEW,
# dedicated collections just for this topic's demos — learning_posts_embedded,
# learning_posts_referenced, learning_comments_referenced — kept apart
# from learning_tasks and anything else already in that database.
MONGODB_DATABASE_URL="mongodb+srv://user:password@cluster.mongodb.net/express"
