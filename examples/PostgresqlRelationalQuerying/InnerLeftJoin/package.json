{
  "name": "inner-left-join-example",
  "private": true,
  "version": "0.1.0",
  "description": "Real proof of INNER JOIN vs LEFT JOIN: the same two tables, two real queries, two genuinely different result sets.",
  "type": "commonjs",
  "main": "server.js",
  "scripts": {
    "setup": "node setup.js",
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "pg": "^8.22.0"
  }
}
