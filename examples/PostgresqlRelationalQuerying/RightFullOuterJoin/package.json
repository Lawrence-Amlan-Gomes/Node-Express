{
  "name": "right-full-outer-join-example",
  "private": true,
  "version": "0.1.0",
  "description": "Real proof of RIGHT JOIN and FULL OUTER JOIN, using a real guest order (no customer at all) alongside a real customer with no orders — the two kinds of unmatched row a FULL OUTER JOIN refuses to lose.",
  "type": "commonjs",
  "main": "server.js",
  "scripts": {
    "setup": "node setup.js",
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "pg": "^8.22.0"
  }
}
