{
  "name": "primary-foreign-keys-example",
  "private": true,
  "version": "0.1.0",
  "description": "Real proof of primary keys and foreign keys: a real customers/orders relationship where Postgres itself rejects an order pointing at a customer that does not exist.",
  "type": "commonjs",
  "main": "server.js",
  "scripts": {
    "setup": "node setup.js",
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "pg": "^8.22.0"
  }
}
