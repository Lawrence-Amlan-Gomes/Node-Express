{
  "name": "constraints-data-integrity-example",
  "private": true,
  "version": "0.1.0",
  "description": "Real proof of 4 real Postgres constraints: ON DELETE RESTRICT (blocks a parent delete), ON DELETE CASCADE (auto-deletes children), CHECK (rejects a bad value), and UNIQUE (rejects a duplicate) — each one triggered for real, never simulated.",
  "type": "commonjs",
  "main": "server.js",
  "scripts": {
    "setup": "node setup.js",
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "pg": "^8.22.0"
  }
}
