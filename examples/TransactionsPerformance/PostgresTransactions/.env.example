# Real .env files (git-ignored by this project's root .gitignore) hold
# secrets and per-environment values — never commit a real one.
#
# A dedicated schema for this topic's Postgres examples — separate from
# both "node_express_learning" (Connecting Real Databases) and
# "node_express_learning_migrations" (Migrations & Schema Design).
DATABASE_URL="postgres://user:password@host:5432/postgres?schema=node_express_learning_transactions"
