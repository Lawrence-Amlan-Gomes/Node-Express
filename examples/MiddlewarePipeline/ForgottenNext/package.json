{
  "name": "forgotten-next-example",
  "private": true,
  "version": "0.1.0",
  "description": "Real, runnable proof that a middleware which never calls next() (and never sends a response) hangs a request forever.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "demo": "node demo.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}
