{
  "name": "presigned-s3-uploads-example",
  "private": true,
  "version": "0.1.0",
  "description": "Real presigned-URL uploads against a real, self-hosted S3-compatible (MinIO) bucket — the client uploads file bytes directly to storage, never through Express.",
  "type": "module",
  "main": "demo.js",
  "scripts": {
    "start": "node server.js",
    "demo": "node demo.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.723.0",
    "@aws-sdk/s3-request-presigner": "^3.723.0",
    "dotenv": "^17.4.2",
    "express": "^5.2.1"
  }
}
