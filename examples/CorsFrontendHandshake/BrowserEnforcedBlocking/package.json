{
  "name": "browser-enforced-blocking-example",
  "private": true,
  "version": "0.1.0",
  "description": "Real proof, using a real headless browser, that CORS blocking happens entirely on the CLIENT side — the server always answers, the browser decides whether JS ever gets to see it.",
  "type": "commonjs",
  "main": "demo.js",
  "scripts": {
    "postinstall": "playwright install chromium",
    "demo": "node demo.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "playwright": "^1.61.1"
  }
}
