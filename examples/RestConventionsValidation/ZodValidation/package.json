{
  "name": "zod-validation-example",
  "private": true,
  "version": "0.1.0",
  "description": "A real Express route validating its real request body with zod, returning real per-field error details on invalid input.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "demo": "node demo.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^5.2.1",
    "zod": "^4.4.3"
  }
}
