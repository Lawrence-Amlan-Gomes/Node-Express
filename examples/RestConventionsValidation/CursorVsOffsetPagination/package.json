{
  "name": "cursor-vs-offset-pagination-example",
  "private": true,
  "version": "0.1.0",
  "description": "Real, measured proof that offset pagination gets slower as the offset grows, while cursor pagination stays fast — against a real remote PostgreSQL database with a real 100,000-row table.",
  "type": "commonjs",
  "main": "demo.js",
  "scripts": {
    "start": "node server.js",
    "demo": "node demo.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@prisma/client": "^6.19.0",
    "dotenv": "^17.4.2",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "prisma": "^6.19.0"
  }
}
