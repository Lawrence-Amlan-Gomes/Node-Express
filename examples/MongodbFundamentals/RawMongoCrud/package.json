{
  "name": "raw-mongo-crud-example",
  "private": true,
  "version": "0.1.0",
  "description": "Real, runnable proof of full CRUD (insertOne/findOne/find/findOneAndUpdate/findOneAndDelete) against real MongoDB Atlas with the native driver, no ODM, including the real ObjectId conversion every id-based route needs.",
  "type": "commonjs",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "mongodb": "^7.5.0"
  }
}
