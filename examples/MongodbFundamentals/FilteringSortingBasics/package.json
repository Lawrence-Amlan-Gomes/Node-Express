{
  "name": "filtering-sorting-basics-mongo-example",
  "private": true,
  "version": "0.1.0",
  "description": "Real, runnable proof of filter/sort/paginate against real MongoDB Atlas with the native driver — find()'s filter object, .sort(), .limit(), .skip() — built safely from real query-string parameters, never by spreading req.query directly.",
  "type": "commonjs",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "mongodb": "^7.5.0"
  }
}
