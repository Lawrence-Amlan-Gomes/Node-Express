{
  "name": "aggregation-basics-mongo-example",
  "private": true,
  "version": "0.1.0",
  "description": "Real, runnable proof of a $group aggregation pipeline against real MongoDB Atlas with the native driver — one real summary document per category, computed with $sum/$avg/$min/$max, all real JS numbers, no string-casting the way Postgres's COUNT/NUMERIC needs.",
  "type": "commonjs",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "mongodb": "^7.5.0"
  }
}
