// The SAME real fields as the REST section's data — deliberately, so the
// two sections are a genuine apples-to-apples comparison, not a rigged one.
export const USERS = [
  { id: "1", name: "Lawrence", email: "lawrence@example.com", bio: "A long real bio nobody asked for in this screen." },
];

export const ORDERS = [{ id: "1", userId: "1", item: "Mechanical Keyboard" }];
