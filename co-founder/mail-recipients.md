# Mail Recipients

Owned by `skillCoFounderMentor.md` — read before every `End Today` mail send (Section 7), don't hardcode paths from memory. This is the OUTBOX address list. The INBOX is always this project's own root `mail-box/` folder, not listed here.

Default behavior: send a mail to every recipient below at every `End Today`, unless the user says otherwise when prompting that specific `End Today` (e.g. "skip mail this time," "only send to X").

## Recipients

- **MySelf** — `/Users/lawrencealangomes/Documents/MySelf/mail-box/skillsUpdateMentor` (a specific existing subfolder — write here, not into `MySelf/mail-box/` directly; that folder has other subfolders for other sender projects, not ours to touch)
- **JobCrack** — `/Users/lawrencealangomes/Documents/JobCrack/mail-box`
- **Lawrence Amlan Gomes** — `/Users/lawrencealangomes/Documents/Lawrence Amlan Gomes/mail-box`

## Rules

- These are write-only targets. **Never read, delete, or modify a file already sitting in one of these folders** — it belongs to that project's own inbox-management, not to us, even if it looks stale or related.
- Create the target folder if the exact given path doesn't exist yet (confirmed 2026-07-30: `JobCrack/mail-box` existed but was empty; `MySelf/mail-box/skillsUpdateMentor` and `Lawrence Amlan Gomes/mail-box` already had unrelated pre-existing mail from other senders — left untouched).
- One mail file per recipient per session is the default; more than one is fine only when there are genuinely separate topics worth splitting, not as a default habit.
- See `skillCoFounderMentor.md` Section 7 for the full protocol (naming, required identity/timestamp header, expected level of detail).

## Change log

- 2026-07-30 — Recipient list established, per the user's direct instruction. Protocol added the same session.
