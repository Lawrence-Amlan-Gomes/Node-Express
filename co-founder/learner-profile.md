# Learner Profile

Owned by `skillCoFounderMentor.md`. Started 2026-07-13, onboarding closed for real the same day once verified evidence replaced guesswork (see below) — don't reopen the deferred-onboarding framing.

- **Background — NOT a beginner in any language-level sense, verified directly, not assumed.** On 2026-07-13 the user briefly nested a separate sibling project ("JavaScript", its own git repo, normally at `/Users/lawrencealangomes/Documents/JavaScript`) into this folder so I could read its state — same pattern as the earlier "Next Js" sibling. Its `co-founder/curriculum.md` and `STATE.md` show a genuinely comprehensive, completed JS + TypeScript curriculum: all of Foundations (variables, hoisting, operators, functions, arrays, higher-order functions, objects), Intermediate (closures, debounce/throttle, `this` binding, prototypes, destructuring/spread/rest, ES Modules, ES6+, classes), Async JavaScript (**event loop — call stack, microtask vs macrotask queue, execution order, already fully covered generically** — see the callout below), Promises, async/await, error handling, Advanced (design patterns, functional programming, memory/performance, browser APIs), Interview Prep, and a full 8-topic TypeScript Foundations phase (generics, utility types, discriminated unions, `.d.ts` declarations, the works). A handful of newer topics (regex, AbortController, generators, Symbol/Proxy/Reflect, frontend security, web perf, testing fundamentals, modern TS idioms) were added 2026-07-10 from a gap-analysis pass but not yet taught — irrelevant to backend work either way.
- **Direct overlap with this project's Stage A, topic 1 — already handled once, don't repeat the mistake.** The JS project's `exercises/event-loop.js` already drills the exact generic rule (sync code first, microtasks fully drain before any macrotask, chained-promise ordering) that this project's own `what-is-nodejs` topic initially over-explained. Revised 2026-07-13 to acknowledge that prior mastery explicitly and spend the topic's real content budget on what Node specifically adds on top: the V8/libuv split, libuv's actual named phases (timers/pending callbacks/poll/check/close callbacks), the thread pool, and `setImmediate` (a Node-only API with no browser equivalent, so genuinely new). **Standing rule going forward: before teaching any concept that sounds JS-fundamentals-adjacent (closures, prototypes, `this`, promises, async/await, modules, classes), assume it's already mastered per this file and pivot straight to the Node/Express-specific delta — don't re-derive the JS-level basics from scratch.**
- **Wider context: this is one project in a deliberate multi-project learning system**, each with its own Claude Code "co-founder mentor" persona, consistent conventions across all of them (interactive app-as-curriculum — sidebar nav grouped by phase + right-side content pages; exercises/examples as fully-worked runnable files, never fill-in-the-blank; every guide ends with an "Interview Angle" section; real code only, no fake stand-ins; status advances on the user's word alone, no re-verification quizzing after he says "done"). Known siblings so far: **JavaScript** (this one, JS+TS language fundamentals, essentially complete), a separate **React + Tailwind** project (mentioned as already underway, TypeScript-with-React specifically deferred there in favor of mastering plain TS in the JS project first), **Next Js** (App Router, Server Components, rendering — Stage A+B mastered per its own state), and a **portfolio** project (a real deployed site, synced with the JS project at least once via a "Start Chat/End Chat" manual relay protocol between two separate Claude Code sessions). This project (Node + Express) is the newest addition to that system. Not implementing the cross-project relay protocol here unless asked — just background to know it exists.
- **This project's target:** Junior → Mid → Senior **Node.js + Express backend** developer — the inverse framing of the Next.js sibling ("frontend dev who reads APIs, doesn't build backends"). Here: become someone who can credibly hold the backend seat and work *with* a frontend team (API contracts, CORS, what a frontend dev needs from you) — the existing frontend background is a real differentiator to lean into, not ignore.
- **Preferred format:** Confirmed by consistent pattern across every sibling project, not just assumed: hands-on, real runnable code over long lectures, delivered through an interactive app (sidebar + topic pages), never fill-in-the-blank exercises — always fully worked, runnable files. Applied here as the Next.js viewer app + `examples/`/`example-runners/` pattern (see `build-conventions.md`).
- **Timeline / pressure:** Not yet stated for this specific project. The JS project shows sustained, near-daily sessions over roughly two weeks (2026-07-03 through 2026-07-10) — reasonable to expect similar pace here, but don't assume without asking if it matters for a specific decision.
- **Code-review sharpness — confirmed pattern across multiple sibling projects, not a one-off.** Both the Next.js and JavaScript projects show the same trait repeatedly: catches subtle real-vs-fake mismatches immediately, never rubber-stamps "done," corrects mentor mistakes plainly when they happen (e.g. this project's own initial no-UI misstep on 2026-07-13). Build real, verified code the first time — see `build-conventions.md`.
