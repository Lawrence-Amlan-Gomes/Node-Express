# Current State

Owned by `skillCoFounderMentor.md`. Live snapshot — always **overwrite** during `End Today`, never just append. First file read on every `@skillCoFounderMentor.md` activation.

- **Last session:** 2026-07-16, closed via `End Today`.
- **What got built, in order:**
  1. **Resolved the database-environment blocker carried over from 2026-07-15.** Instead of local Docker/Homebrew, the user pointed to real infrastructure he already has: a PostgreSQL server on Coolify (`185.201.8.71`) and a MongoDB Atlas cluster — both already shared with other real projects. Verified connectivity to both directly before building anything, and confirmed real existing data on each (Postgres `public` schema had real `clicks`/`links`/`users` tables from another app; Atlas already had ~14 other real databases). Established a standing convention: every mini-project that uses `prisma db push`/`migrate` gets its OWN dedicated Postgres schema — never shared, not even across mini-projects in the same topic — after two separate real incidents this session where sharing a schema caused real data loss/corruption (see Open Gaps → resolved, and `build-conventions.md`).
  2. **Stage C completed — all 4 topics built and mastered:**
     - **"Connecting Real Databases: PostgreSQL & MongoDB"** — real Prisma CRUD against the real Coolify Postgres (`node_express_learning` schema), real Mongoose CRUD against real Atlas (`express` database, `learning_tasks` collection), and a side-by-side comparison proving the real shape difference (Postgres's plain columns vs. Mongo's `_id`/`__v`).
     - **"Migrations & Schema Design"** — two real, timestamped Prisma Migrate migrations actually applied to a dedicated schema, proving a pre-existing row survived a schema change with its new column's default auto-applied; plus a real MongoDB embed-vs-reference comparison with a measured 1-query-vs-2-query difference.
     - **"Transactions & Query Performance"** — real Prisma `$transaction` and Mongoose `session.withTransaction` rollback proofs (a forced mid-transaction failure undoing an already-executed write, on both databases), plus a real, measured N+1 query-count fix (4 queries → 2, verified via Prisma's own query log — confirmed `include` is NOT a single JOIN, it's 2 batched queries).
  3. **Stage D completed — all 4 topics built and mastered:**
     - **"REST Conventions & Validation"** — real status codes on a real `/api/v1` Express API, a measured ~300x real speed gap between offset and cursor pagination (seeded 100,000 real Postgres rows, measured with `EXPLAIN ANALYZE`), a real idempotency-key implementation stopping a retried charge from double-billing, and real zod validation (verified Zod 4's actual API, which changed from v3).
     - **"Authentication Patterns"** — real bcrypt salted hashing (verified same password → different hashes), a full real `express-session` login/protected-route/logout cycle, and real JWT signing/verification with a genuine tamper-rejection proof.
     - **"CORS — the Frontend Handshake"** — the first topic in this project needing a real headless browser (Playwright + Chromium), since CORS turned out to be enforced only by the browser (verified: Node's own `fetch()` sends no preflight and blocks nothing at all). Captured the exact real Chromium console error text for a blocked cross-origin request, and proved the real credentials opt-in rule (client `credentials: 'include'` AND server `Access-Control-Allow-Credentials`, both required).
     - **"OWASP API Security & Rate Limiting"** — a real BOLA (Broken Object Level Authorization) data leak and its fix, a real SQL injection login bypass and its parameterized-query fix, real rate limiting (5-per-window, verified 429s), and a real helmet header before/after comparison.
  4. **A significant content-quality correction, caught by the user twice in one sitting.** After all 8 Stage C/D topics were built, the user asked for every section's explanatory prose to use simple, everyday vocabulary and short sentences — "like explaining to a child." The first rewrite pass was **rejected directly** ("stupid you changed nothing") because it only swapped in easier-sounding synonyms while keeping the same long, jargon-stacked sentence structure. Redone properly the second time with a concrete, stricter standard: break every sentence at its natural joints into short, single-idea sentences (~10-15 words each), explain or replace jargon beyond basic recurring backend vocabulary (query/schema/endpoint are fine; "polyglot persistence," "atomicity," "idempotent," "instrumentation," "namespace," "drift" are not, without explanation). This standard is now saved to memory (`feedback_beginner_comments_and_demos.md`) so future topics get it right from the first draft.
- **Active phase:** Stage C and Stage D are both now **fully mastered**. Next up: **Stage E — Testing, Tooling & Production Readiness**, starting with its first topic (per `roadmap.md`: unit + integration testing with Jest/Vitest and Supertest).
- **Active project:** The Node + Express Playground app itself (`src/app/`) — this app IS the curriculum.
- **Real infrastructure:** Stage A (5/5 mastered), Stage B (5/5 mastered), Stage C (4/4 mastered), Stage D (4/4 mastered) — **20 of the full curriculum's topics mastered so far.** Every built topic follows the one-mini-project-per-section convention, the "wrong code only in comments" rule, and (from this session onward) the short-sentence/plain-vocabulary prose standard. `npm install`/`typecheck`/`lint`/`build` all verified clean as of session close, across every topic including the two Playwright-based CORS examples and all real Postgres/MongoDB-backed examples.
- **Next session should start with:** Begin Stage E, topic 1 — **"Testing an Express App"** (Jest or Vitest + Supertest, per `roadmap.md`). No environment blockers expected — this is pure Node/Express tooling, no new external infrastructure decision needed. Follow the same real-code, one-mini-project-per-section, short-sentence-prose discipline established across Stage C/D.
- **Open gaps / weak spots:** None currently flagged by the user beyond the prose-simplification correction (already fixed and now a standing, memorized rule — not an open gap anymore). On the authoring side: when writing prose for Stage E/F topics, proactively self-check every paragraph against the new short-sentence standard *before* showing it to the user, rather than waiting to be corrected a second time.
- **Momentum notes carried forward:**
  1. **Real infrastructure discipline pays off, but requires real care.** Using the user's actual shared Coolify Postgres and MongoDB Atlas (rather than local throwaway databases) made every demo more realistic, but surfaced two genuine near-misses this session: `prisma migrate dev` detecting an unrelated table as "drift" and wanting to reset a whole schema, and `prisma db push` silently dropping/renaming a sibling mini-project's table when run against a shared schema. Both are now hard-won, documented rules — never mix Prisma schema-management approaches, never share a schema across independent Prisma projects.
  2. **CORS taught a broader lesson**: when a topic's real behavior is enforced by something other than Node/Express itself (a browser, an OS mechanism, a client library), a plain Node script proving it is not real evidence — reach for the actual enforcing environment (here, a real headless browser via Playwright) rather than faking it. Keep this in mind for any future topic that smells browser- or OS-enforced.
  3. **Verification discipline keeps paying off**: the ~300x real pagination gap, the real N+1 query-count fix (2 vs 4, not "a single JOIN" as commonly assumed), and Zod 4's actual changed API were all things that could have been asserted from memory instead of measured/checked directly — keep defaulting to running things for real.
  4. **The user's code-review sharpness extends to prose quality, not just code correctness** — he caught a shallow, cosmetic rewrite attempt immediately and called it out plainly. Don't mistake "technically did what was asked" for "actually did what was asked" — reread rewritten content against the concrete standard before presenting it as done.
