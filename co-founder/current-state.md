# Current State

Owned by `skillCoFounderMentor.md`. Live snapshot — always **overwrite** during `End Today`, never just append. First file read on every `@skillCoFounderMentor.md` activation.

- **Last session:** 2026-07-13, closed via `End Today`. This was the project's first real session — everything below happened in one sitting.
- **What got built, in order:**
  1. The mentor system itself: `skillCoFounderMentor.md`, `skillGit.md`, root `CLAUDE.md`, and `co-founder/` (`current-state.md`, `session-log.md`, `learner-profile.md`, `roadmap.md`, `build-conventions.md`, `git-log.md`, `ports.md`).
  2. **Course-corrected once**, then rebuilt properly: initially scaffolded with no interactive delivery app at all (misread "backend-only" as "no UI needed"). Corrected after the user pointed at a sibling "Next Js" mentor project's real shell (Sidebar/Dashboard/RoadMap/topic pages) as the standing expectation across his whole learning-project system, not something "backend" overrides. Rebuilt as a real Next.js + TypeScript + Tailwind v4 app, mirroring that shell 1:1 where the concept transfers, with one deliberate departure: `TerminalFrame` replaces `BrowserFrame`, since backend demos prove themselves via real command + real captured output ("Live Output from Real Code"), not a rendered page.
  3. Built and shipped the first real topic, **"What is Node.js, really"** (`src/app/topics/what-is-nodejs/page.tsx`, status `in-progress`) — V8/libuv layering, libuv's actual named-phase model (timers → pending callbacks → poll → check → close callbacks), the thread pool, and a genuinely live demo (`EventLoopOrderRunner.tsx`, spawns the real example script server-side via `execSync` on every render/rerun).
  4. After the user brought in a second sibling ("JavaScript," a completed JS+TS curriculum) for reference, discovered and fixed real overlap: the topic had been over-explaining the generic event-loop rule Lawrence already mastered there. Revised to acknowledge that and focus on what's actually Node-specific.
  5. Ran a ~10-query web-research pass (2026-07-13) checking the whole roadmap against current industry/interview standards. Added 8 concrete items across Stages A/D/E/F (TypeScript-with-Node topic, Express 5 pinned as the explicit target, pagination + idempotency keys, OWASP API Security framing, a debugging/memory-profiling topic, a sharpened cluster-vs-worker_threads distinction, a real `background-jobs-queues` topic card the prose roadmap had described but `curriculum.ts` never actually shipped, and a lightweight GraphQL/tRPC awareness topic). Full trail in `co-founder/roadmap.md`.
  6. Fixed a real visual bug: `globals.css` never set a default text `color` on `body`, so unstyled text nodes rendered browser-default black on the black background (worst in the libuv-phases diagram's raw numbering text). Fixed at the root (`color: var(--color-body)` on `body`), not just the one spot.
- **Active phase:** Stage A in progress. Topic 1 (`what-is-nodejs`) is built and revised twice (once for the frontend-vs-backend course-correction's downstream effects, once for the JS-overlap fix) but still **not confirmed `mastered`** — no turn-by-turn teaching has actually happened yet with the user; all authoring was done solo between corrections/requests.
- **Active project:** The Node + Express Playground app itself (`src/app/`) — this app IS the curriculum.
- **Confirmed technical gotchas this session** (all recorded in `co-founder/build-conventions.md`, don't rediscover): (1) `execFileSync("node", [pathString])` inside a Server Component fails `next build` with a false "server relative imports" error regardless of how the path is built — use `execSync("node file.js", { cwd })` instead. (2) Running `rm -rf .next && npm run build` while `npm run dev` is live on the same `.next` directory corrupts its cache — stop the dev server first. (3) Any new temporary sibling reference folder nested into this project needs to be added to `tsconfig.json` exclude, `eslint.config.mjs` ignores, AND `.gitignore` immediately, all three — missing it on `JavaScript/` caused 713 stray lint errors. (4) `body` needs an explicit default `color`, or unstyled text is invisible on this dark theme.
- **Real infrastructure:** full Next.js app scaffold (pinned versions matching the sibling: Next 16.2.10, React 19.2.7, Tailwind 4.3.2, TypeScript 6.0.3, ESLint 9.39.4); `src/data/curriculum.ts` as the single source of truth (now Stage A: 5 topics, B: 4, C: 4, D: 4, E: 5, F: 6); `examples/NodeRuntimeFoundations/EventLoopOrder/` + its runner as the one shipped example so far. `npm install`/`typecheck`/`lint`/`build` all verified clean as of session close.
- **Next session should start with:** Ask the user whether "What is Node.js, really" makes sense well enough to mark `mastered` — nothing's been confirmed by him yet. If yes, move to Stage A topic 2, **TypeScript with Node.js** (newly added this session, ahead of "Modules, npm & package.json" in build order — confirm with him whether he wants TS set up before or after the plain-JS module-system topic, since the research pass added it but didn't reorder anything). Also worth a quick temperature check on pace/depth preference, since onboarding (JS level / backend experience / teaching style) was deferred twice now and all calibration so far has come from reading sibling projects, not him directly.
- **Open gaps / weak spots:** None on the user's side — no teaching has happened turn-by-turn yet. On the authoring side: this session made one real scope misread (no-UI) that needed a direct correction, and one redundancy misread (over-teaching the generic event loop) — both fixed, but the pattern worth watching is "check siblings and think through what 'backend/frontend' framing actually implies before building," not just after being corrected.
- **Momentum notes carried forward:** (1) The app-as-curriculum shell is a standing expectation across this user's whole multi-project system — never re-derive that from a subject-matter framing again. (2) His learning projects genuinely overlap in content — always read a sibling's real state before writing new topic content, not just when offered. (3) He catches real-vs-fake mismatches and visual/config bugs immediately and directly — build things correctly the first time, and when corrected, fix the actual root cause, not just the visible symptom (as with the body-color CSS fix vs patching only the one diagram).
