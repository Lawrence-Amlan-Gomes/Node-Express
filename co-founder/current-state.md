# Current State

Owned by `skillCoFounderMentor.md`. Live snapshot — always **overwrite** during `End Today`, never just append. First file read on every `@skillCoFounderMentor.md` activation.

- **Last session:** 2026-07-14, closed via `End Today`. Spanned from 2026-07-13's late session into 2026-07-14 (date rolled over mid-session).
- **What got built, in order:**
  1. **Repo initialized for real**: `git init`, remote added (`github.com/Lawrence-Amlan-Gomes/Node-Express.git`), first commit pushed to `origin main` via `skillGit.md`.
  2. Confirmed **"What is Node.js, really" mastered** (first real user confirmation of the whole project).
  3. Built and got mastered on the rest of **Stage A**: "Modules, npm & package.json" (CJS/ESM interop, including the require()-can-now-load-sync-ESM behavior confirmed on Node 22.12+), "TypeScript with Node.js" (native strip-types vs tsx vs tsc, the type-stripping-≠-type-checking gap), "Core Modules" (path/fs/Buffer/structuredClone/events/process/Headers/FormData/fetch, a real local http server), and "Async Patterns on a Server" (callback/promise/async-await, a measured real blocking-vs-non-blocking demo). **Stage A is fully mastered.**
  4. Moved into **Stage B**: built and mastered "App & Router Basics (Express 5)" and "The Middleware Pipeline." Added a brand-new topic mid-stream at the user's direct request — **"API Testing Tools: curl, Postman & Thunder Client"** (a real gap the roadmap hadn't named) — built and mastered the same session.
  5. **Several real corrections became standing rules**, all now in `co-founder/build-conventions.md`:
     - FilePointers must resolve inside a topic's own `examples/` folder, never point at root project files (caught on "Modules, npm & package.json").
     - Example code needs generous plain-English comments explaining backend/framework concepts, and every content section needs its own live demo — not just some sections (caught on "App & Router Basics").
     - **The big one**: every distinct page section needs its OWN separate, self-contained mini-project — never a shared server/example split across sections via multiple demo scripts. This was retroactively fixed across all three built Stage B topics at the time (Middleware Pipeline → OrderOfExecution/AuthOrderMatters/ForgottenNext; App & Router Basics → CreatingTheApp/RoutesParamsQueryBody/NamedWildcards; API Testing Tools → CurlWorkflow/PostmanCollection/HttpRequestFile), deleting the old shared folders entirely.
  6. Added the app favicon (`src/app/icon.svg`, matching the sidebar's `NodeLogo`), a sidebar text-wrapping fix (long topic titles now wrap to 2 lines instead of truncating), and a visual, orange-branded Postman UI mockup (`src/components/PostmanMockUI.tsx`) for the API Testing Tools topic — populated with real, already-verified response data, not invented.
  7. Caught and documented several real technical gotchas this session (all in `build-conventions.md`): Express 5's wildcard route change (bare `*` now throws; named wildcards return an array, not a string), a naive ESM "run directly vs imported" check that silently never matched (`pathToFileURL` is the fix), and a same-process blocking-demo race (racing two different routes is flaky; measuring total time for two requests to the SAME route is the reliable fix).
- **Active phase:** Stage B in progress — 3 of 5 topics mastered (App & Router Basics, Middleware Pipeline, API Testing Tools). Two topics remain unbuilt: **Error Handling in Express** and **Project Structure & Config**.
- **Active project:** The Node + Express Playground app itself (`src/app/`) — this app IS the curriculum.
- **Confirmed technical gotchas this session** (all recorded in `build-conventions.md`, don't rediscover): (1) Express 5 route matching genuinely rejects a bare `*` wildcard (`Missing parameter name...`); named wildcards (`/files/*splat`) are required, and their match comes back as a real array of segments. (2) A naive ESM main-module check (`import.meta.url === \`file://${process.argv[1]}\``) silently never matches because `process.argv[1]` is often relative while `import.meta.url` is always absolute — use `pathToFileURL(process.argv[1]).href` instead. (3) Racing two different routes to prove "blocking freezes the server" is genuinely flaky (confirmed: sometimes the wrong request wins the race) — measure total time for two concurrent requests to the SAME route instead. (4) `tsc` still emits output by default even when a type error exists elsewhere in the compiled set (`noEmitOnError` is what changes that). (5) Root `tsconfig.json`/`eslint.config.mjs` must exclude `examples/` — the first example with real `.ts`/dependency-bearing files leaked into the root app's own typecheck/lint otherwise.
- **Real infrastructure:** full Next.js app scaffold, real git remote connected and pushed. `src/data/curriculum.ts`: Stage A (5/5 mastered), Stage B (3/5 mastered, 2 unbuilt), Stage C–F all `not-started`. Every built topic has its own self-contained `examples/<Topic>/<SectionSpecificName>/` mini-project per section (the corrected convention) — `npm install`/`typecheck`/`lint`/`build` all verified clean as of session close.
- **Next session should start with:** Build Stage B topic 4, **"Error Handling in Express"** (Express 5's automatic rejected-promise-to-error-middleware forwarding as the modern default, contrasted with the manual try/catch pattern it replaces — still worth knowing as legacy-codebase awareness). Remember the one-mini-project-per-section rule from the very start when planning this topic's sections — don't build one shared example and split it after the fact again.
- **Open gaps / weak spots:** None flagged on the user's side this session — he's been catching real, substantive structural issues immediately and directly (root-file FilePointers, missing beginner comments, missing per-section demos, and the big one: shared mini-projects across sections). On the authoring side: the pattern to keep watching is "decide the sections FIRST, then build," not build-then-split — this was the single biggest rework of the session and is now the standing rule.
- **Momentum notes carried forward:** (1) The one-mini-project-per-section rule is now the most important standing build convention — apply it from the start on every future topic, in this project and any sibling using the same app-as-curriculum shell. (2) The user directly drives roadmap completeness checks (he caught the missing API-testing-tools gap himself) — take "does this roadmap cover X" questions seriously as real gap-analysis prompts, not just reassurance-seeking. (3) He wants things demonstrated visually when the subject matter is itself a GUI tool (the Postman mockup) — don't default to terminal-only output when a different kind of illustration would teach the concept better, as long as the underlying data stays genuinely verified.
