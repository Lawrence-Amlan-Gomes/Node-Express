# Current State

Owned by `skillCoFounderMentor.md`. Live snapshot — always **overwrite** during `End Today`, never just append. First file read on every `@skillCoFounderMentor.md` activation.

- **Last session:** 2026-07-31 (2nd session that date), closed via `End Today`. Curriculum status at that point: the original 31 topics + PostgreSQL Fundamentals remain mastered; MongoDB Fundamentals stayed `in-progress`, paused mid-initiative.
- **2026-08-03, later same session:** the user decided Stage G — "Company-Specific Interview Prep" (added 2026-07-31, one topic built: "Metaora") — was no longer wanted, and asked for it to be fully deleted, not just hidden. Removed: the `stage-g` entry from `curriculum.ts`, `src/app/topics/metaora/`, `examples/Metaora/` (4 mini-projects), and every current-state reference to it in this file and `roadmap.md`. **`build-conventions.md`'s historical entries that reference Metaora as the incident that triggered the `DataTable`/alias-teaching standing rules were left alone** — those rules still apply project-wide (used by "PostgreSQL Fundamentals" and "PostgreSQL Relational Querying" among others) and the entries are accurate history of why they exist, not a claim that Metaora currently exists. Verified clean: `typecheck`, `lint`, a fresh `rm -rf .next && npm run build` (39/39 pages, metaora route gone), live `curl` confirming `/topics/metaora` now falls through to the generic unbuilt-topic stub like any other non-curriculum slug.

## 2026-08-03 session: SQL ground-zero teaching correction + "PostgreSQL Relational Querying" built

Session opened with mail inbox empty, dev server started on port 3004 (3000-3003 all occupied by unrelated sibling projects). Before any curriculum work, the user pasted a real ChatGPT conversation teaching him SQL and directly called this project's own "PostgreSQL Fundamentals" page worse — despite it already being `mastered`, technically correct, and already using real `DataTable`s. Diagnosis: the page never opened any concept with a real-world analogy or a plain-English translation of the keyword itself (ChatGPT's version did, every time — "SELECT means 'show me'", library/spreadsheet analogies, "imagine your boss asks..." motivating scenarios) before diving into mechanics. New standing rule, full detail in `build-conventions.md`'s matching entry and the `feedback_sql_ground_zero_analogy` memory: any jargon-named concept needs an analogy + one-line plain-English translation BEFORE `ConceptBreakdown`/mechanics, on every future topic, not just SQL ones.

User confirmed scope via direct question: apply this BOTH retroactively to "PostgreSQL Fundamentals" AND to the next topic being built. Both done this session:

1. **"PostgreSQL Fundamentals" rebuilt in place** (no new mini-projects needed — narrative/prose additions only): section 1 gets a database/table/SQL ground-zero analogy; section 2 gets a real, freshly-captured SELECT-*-vs-SELECT-column before/after `DataTable` pair plus plain-English CRUD keyword translations; section 3 gets a "50,000 movies" WHERE-motivating scenario; section 4 gets a "boss asks per-category totals" GROUP BY-motivating scenario. Verified via `typecheck`/`lint`/a clean `rm -rf .next && npm run build` and live `curl` 200.

2. **New topic built from scratch: "PostgreSQL Relational Querying"** (`hasPage: true`, `status: "in-progress"`, awaiting the user's own mastery confirmation) — the next unblocked item in the paused Postgres/MongoDB initiative, now built with the ground-zero standard from the start. 5 real sections, 5 real mini-projects under `examples/PostgresqlRelationalQuerying/`, each its own dedicated Postgres schema on the live Coolify server, fixed ports 4143-4147 (next free block after Metaora's 4142):
   - **PrimaryKeysForeignKeys** (4143, schema `node_express_learning_pg_rel_keys`) — real customers/orders FK relation; a valid order succeeds, an order pointing at a nonexistent customer is genuinely rejected by Postgres (real error code 23503), caught and returned as a clean 400.
   - **InnerLeftJoin** (4144, schema `node_express_learning_pg_rel_join`) — same real tables, INNER JOIN vs LEFT JOIN proven side by side: a customer with zero orders vanishes from the INNER JOIN result, stays with `product: null` in the LEFT JOIN result.
   - **RightFullOuterJoin** (4145, schema `node_express_learning_pg_rel_outer`) — customer_id made nullable to support a real "guest order" (no customer at all); RIGHT JOIN drops the guest order (mirrors LEFT JOIN, table order flipped), FULL OUTER JOIN brings it back with `name: null` — the two kinds of unmatched row FULL OUTER refuses to lose.
   - **Subqueries** (4146, schema `node_express_learning_pg_rel_subquery`) — a real scalar subquery (`WHERE amount > (SELECT AVG(amount) ...)`) and a real list subquery (`WHERE id IN (SELECT customer_id ...)`), both verified against real seeded amounts (avg 107.50, correctly splitting above/below).
   - **ConstraintsDataIntegrity** (4147, schema `node_express_learning_pg_rel_constraints`) — 4 real constraints proven with 4 real Postgres error codes: ON DELETE RESTRICT blocks a parent delete (23503/409), ON DELETE CASCADE on a separate table pair auto-deletes the child (verified via a follow-up GET showing the order genuinely gone), CHECK rejects a negative price (23514/400), UNIQUE rejects a duplicate product name (23505/409).

   Every mini-project's real behavior was curled and verified against the live Coolify Postgres server before being written into the page (same discipline as every other topic), then cleaned up (test rows deleted) to leave the DB in a fresh state. Page built with real `DataTable`s (source tables first, then result tables, real SQL query text between them, per the existing standing rule), `PostmanCheck` + `PgAdminCheck` on every section, and the new ground-zero analogy/plain-English-translation opening on every section per the new standing rule above. Full app verified clean: `typecheck`, `lint`, a fresh `rm -rf .next && npm run build` (41/41 pages, including the new route), and live `curl` 200s on both `/topics/postgresql-fundamentals` and `/topics/postgresql-relational-querying`.

**Not yet done, flagged for later, not this session:** no headless-Chromium screenshot pass was run on the new page (unlike Metaora's session) — root app has no Playwright tooling wired up at that level; only the mini-projects' own `package.json`s get Playwright per the existing convention. Revisit only if a visual regression is suspected.

## Two more real corrections, later the same session

1. **The SELECT-column explanation still didn't land, even with the before/after `DataTable` pair from item 1 above.** The user asked directly why `SELECT title FROM books` returns only the title column — the page showed two disconnected result tables but never actually walked through the transformation. Fixed on "PostgreSQL Fundamentals": added a JS-analogy bridge (`books.map(b => ({ title: b.title }))`, leveraging the user's confirmed JS mastery per `learner-profile.md`), the full 4-column table shown first, then both queries with their real SQL directly above each result table, plus an explicit callout stating it's the same 3 rows either way. Verified via `typecheck`/`lint`/a clean `rm -rf .next && npm run build` and live `curl` 200.
2. **No table aliases, ever, going forward — a direct, unambiguous preference, now a standing rule (`feedback_no_sql_aliases` memory).** The user said plainly he doesn't want short nicknames like `c`/`o` in JOIN queries. Removed aliases from every real SQL query across all 5 "PostgreSQL Relational Querying" mini-project controllers AND every corresponding page mention (diagram query snippets, `ConceptBreakdown` prose, pgAdmin4 SQL) — every query now spells out the full table name throughout (`customers.name`, `orders.product`), referencing the unqualified table name in ON/SELECT/ORDER BY once introduced via a schema-qualified name in FROM/JOIN (confirmed valid Postgres syntax, verified directly against the live server, not just assumed). Full app re-verified clean afterward (`typecheck`/`lint`/build/curl). **This reverses the earlier alias-teaching pattern from the now-deleted "Metaora" topic** — if any future topic is tempted to teach or use aliases again, don't; this is a standing preference, not a one-off.

## Known carried-forward initiative: PostgreSQL & MongoDB fundamentals-to-advanced (paused, not abandoned)

1. PostgreSQL Fundamentals — **mastered**, and now also has the DataTable/query-code retrofit AND the 2026-08-03 ground-zero analogy retrofit above.
2. MongoDB Fundamentals — **built and verified, paused awaiting the user's mastery confirmation** (unchanged this session — see the 2026-07-31 1st-session entry in `session-log.md` for its own build details).
3. PostgreSQL Relational Querying — **built 2026-08-03, `in-progress`, awaiting the user's own mastery confirmation.** Real primary/foreign keys, INNER/LEFT/RIGHT/FULL OUTER joins, 2 subquery shapes, and all 4 real constraint types (RESTRICT/CASCADE/CHECK/UNIQUE) — see this session's own entry above for full detail. Built with real `DataTable`s + inline query code + the new ground-zero analogy/plain-English-translation standard from the start.
4. MongoDB Schema & Query Patterns — not started.
5. PostgreSQL Advanced Querying & Production Patterns — not started. Same DataTable/query-code AND ground-zero-analogy standing rules apply once reached.
6. MongoDB Advanced Patterns & Production Concerns — not started.

**The user explicitly asked to build these ONE AT A TIME** — this remains true.

**Highest real fixed port across the project: 4147** (`PostgresqlRelationalQuerying/ConstraintsDataIntegrity`) — check this before assigning new ports on the next topic, per the fixed-port-collision gotcha in `build-conventions.md`.

**Next session should start with:**
1. Check the mail inbox (root `mail-box/`) per the standing rule.
2. Ask whether the user has confirmed mastery on "MongoDB Fundamentals" and/or the new "PostgreSQL Relational Querying" — resume whichever the user directs. If both are confirmed, the natural next unblocked item is topic 5, "PostgreSQL Advanced Querying & Production Patterns" (window functions/CTEs, JSONB, connection pooling).

## Open gaps / weak spots (carried forward, still unresolved)

1. REST Conventions, Auth Patterns, and CORS's `PostmanCheck`s still predate the exhaustive-steps/request-response-separation fixes from 2026-07-18. Still just flagged, not actioned.
2. **No actual timed mock interview has ever been run with THIS mentor.** Still a standing offer whenever the user wants a genuine timed, live-reviewed exercise.
3. Two items named in `roadmap.md`'s prose early on — "observability (metrics/tracing basics)" and "behavioral prep narrating this project" — never got dedicated topic cards. Still unresolved.
4. A sibling project, **URL Shortener** (real, deployed, its own Claude Code cofounder), is known to exist — see `learner-profile.md`. Its 5 self-flagged bugs were reported fixed via that project's own cofounder, not verified from here.
5. The DataTable/query-code/alias-teaching fixes from 2026-07-31 have ONLY been applied to PostgreSQL Fundamentals so far — MongoDB Fundamentals, Connecting Real Databases, Migrations & Schema Design, Transactions & Query Performance, and Indexing & Search Performance still have their older card-diagram-only treatment for any relational/tabular content they show. Not yet actioned — revisit only if the user asks for a broader retroactive pass.
6. **New this session (2026-08-03):** the ground-zero analogy/plain-English-keyword-translation standard has ONLY been applied to PostgreSQL Fundamentals and PostgreSQL Relational Querying so far — every other already-shipped topic (all 31 original curriculum topics, MongoDB Fundamentals, and the rest of the Postgres/MongoDB initiative topics) still has the older, more technical-mechanics-first opening. Not yet actioned as a broad retroactive pass — revisit only if the user asks, same posture as gap 5 above.

## Momentum notes carried forward

1. Forcing a real container replacement in a test/demo: don't rely on `docker compose up --force-recreate` alone — force a genuine IP difference via a second container sharing the same `--network-alias` before removing the original (see `build-conventions.md`).
2. A service that fails by refusing/timing out a TCP connect doesn't have deterministic failure timing unless a connect timeout is explicitly configured — treat multiple plausible failure status codes as the same real bug.
3. Standing habits still in force from earlier sessions: proactive port-0/PID-scoping/matchAll-timeout discipline, `npm view` before writing any new dependency version, `@types/node` + `types: ["node"]` for TS examples referencing Node builtins, `error.stdout` for expected non-zero-exit CLI errors, disabling a spawned CLI tool's own internal multiprocess/fork option from the start.
4. A `/bp:judge` (Cavekit) slash command was invoked mid-session by accident/muscle memory — correctly a no-op here, since this project has no Cavekit `context/impl` structure. Not a real gap, just noting it happened in case it recurs.
