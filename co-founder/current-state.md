# Current State

Owned by `skillCoFounderMentor.md`. Live snapshot — always **overwrite** during `End Today`, never just append. First file read on every `@skillCoFounderMentor.md` activation.

- **Last session:** 2026-07-15, closed via `End Today`.
- **What got built, in order:**
  1. **Stage B, topic 4 — "Error Handling in Express"** built and mastered: Express 5's automatic rejected-promise-to-error-middleware forwarding (`AutomaticForwarding`), a real centralized 4-argument error middleware with a custom `AppError` class producing real per-error status codes (`CentralizedErrorMiddleware`), and a real Express 4 install proving the legacy footgun directly (`LegacyTryCatch`). **Genuinely surprising, verified finding**: the textbook description of the Express 4 async footgun ("the request just hangs forever") is outdated on modern Node — verified directly, an uncaught async rejection in Express 4 now **crashes the entire process** (Node 15+ treats unhandled rejections as fatal), not just that one request. Recorded in `build-conventions.md`.
  2. **A significant standing convention correction, caught directly by the user**: shipping deliberately-wrong/anti-pattern code as REAL, executing code in an example (even clearly labeled as wrong) is wrong, because example files double as reference material the user might reuse. Fixed across 4 real instances that session (`MiddlewarePipeline/AuthOrderMatters`'s `/protected-wrong` route, `MiddlewarePipeline/ForgottenNext`'s entire premise, `ErrorHandlingExpress/LegacyTryCatch`'s `/no-trycatch` route, and a 4th, subtler instance — `ErrorHandlingExpress/AutomaticForwarding` shipping a route with NO error middleware at all, silently relying on Express's stack-trace-leaking default handler). **New standing rule, now in `build-conventions.md`: wrong/anti-pattern code (including silent omissions, not just active bugs) lives ONLY as a comment, never as real running code** — the comment still states the real, verified consequence, but the only code that actually executes in a shipped example is the correct, production-worthy version.
  3. **Stage B, topic 5 (final) — "Project Structure & Config"** built and mastered: routes/controllers layering (`LayeredRoutesControllers`), `express.static()` serving real files alongside a JSON API on the same app (`StaticAssetsAndJson`), and real `.env`/`.env.example` config with `dotenv` — plus a verified aside that Node's native `--env-file`/`process.loadEnvFile()` now works too (`EnvConfigWithDotenv`). **Stage B is now fully mastered, all 5 topics.**
  4. **A real curriculum-structure decision**: asked the user how Stage C's database choice should go (previously "TBD together" in `roadmap.md`); the user asked to learn BOTH PostgreSQL and MongoDB in real depth, not just one. Updated `roadmap.md` and `src/data/curriculum.ts` accordingly — every Stage C topic now covers both databases side by side (Prisma for Postgres, Mongoose for MongoDB), same one-mini-project-per-section convention as every other stage.
  5. **Stage C, topic 1 — "SQL vs NoSQL"** built and mastered: the same blog domain modeled as real linked SQLite tables with a real `JOIN` vs a real embedded JS object (`RelationalVsDocumentShape`, using `node:sqlite` — Node's own built-in engine, confirmed working, zero npm install needed for this concept-level topic); a real foreign key constraint actually rejecting a bad insert vs the equivalent unchecked embed (`ForeignKeysVsEmbedding`); and an honest, two-sided real comparison proving each model wins a DIFFERENT real read pattern — one-aggregate fetch favors documents (0 queries vs 2), cross-post search favors relational (1 indexed query vs a manual scan) (`ReadPatternComparison`).
  6. **Started Stage C, topic 2 — "Connecting Real Databases: PostgreSQL & MongoDB" — blocked on a real environment gap, not yet built.** Checked this machine directly: PostgreSQL *client* tools only (`psql`/`initdb`/`pg_ctl` via the `libpq` formula), no real `postgres` server binary at all (`initdb` fails: "program \"postgres\" is needed... but was not found"), no MongoDB installed, no Docker. Asked the user how to proceed (Docker containers for both / native Homebrew installs / check for an already-installed-but-unlinked Docker Desktop / discuss trade-offs first) — the user replied `End Today` instead of answering, ending the session before a choice was made. Full detail in `build-conventions.md`'s matching entry.
- **Active phase:** Stage C in progress — 1 of 4 topics mastered (SQL vs NoSQL). **Next topic, "Connecting Real Databases," is blocked on a real environment decision, not a content decision.**
- **Active project:** The Node + Express Playground app itself (`src/app/`) — this app IS the curriculum.
- **Real infrastructure:** Stage A (5/5 mastered), Stage B (5/5 mastered, fully complete), Stage C (1/4 mastered, 3 unbuilt). Every built topic still follows the one-mini-project-per-section convention, and the newer "wrong code only in comments" rule applies from "Project Structure & Config" onward (and retroactively fixed on the 4 Stage B/C instances found this session). `npm install`/`typecheck`/`lint`/`build` all verified clean as of session close.
- **Next session should start with:** **Resolve the database environment decision first, before writing any Stage C topic-2 code.** Ask again plainly: Docker Desktop (install fresh, or check if it's already installed as a Mac app just not linked to the shell PATH) vs native Homebrew installs (`postgresql@18` + `mongodb-community`, leaves permanent background services) vs discuss trade-offs. Once resolved, build "Connecting Real Databases: PostgreSQL & MongoDB" — real CRUD via Prisma against real Postgres, real CRUD via Mongoose against real MongoDB, compared side by side, same section-per-mini-project discipline as every other topic.
- **Open gaps / weak spots:** None flagged on the user's side this session beyond the wrong-code-in-comments correction (already fully fixed and now a standing rule — not an open gap anymore). On the authoring side: the pattern to keep watching, per that correction, is checking every new example for BOTH active anti-pattern code AND silent omissions (a missing error middleware, a missing validation step) that a real backend dev wouldn't ship — not just the obvious "this route is deliberately broken" case.
- **Momentum notes carried forward:** (1) The "wrong code only in comments, correct code only as real running code" rule is now as important as the one-mini-project-per-section rule — apply it from the start on every future topic, checking for silent omissions too, not just active bugs. (2) The user is willing to expand scope deliberately when it serves real depth (choosing to learn BOTH Postgres and MongoDB rather than picking one) — don't default to the narrower/faster option without asking when a stage's shape is still open. (3) Verification discipline keeps paying off: the Express 4 "crashes, not hangs" finding and the live FK-constraint-rejection proof were both things that could easily have been asserted from memory instead of actually run — keep defaulting to running things for real, including quick real environment checks (like this session's Postgres/Mongo/Docker check) before assuming a topic can be built as planned.
