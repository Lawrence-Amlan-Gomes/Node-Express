# Current State

Owned by `skillCoFounderMentor.md`. Live snapshot — always **overwrite** during `End Today`, never just append. First file read on every `@skillCoFounderMentor.md` activation.

- **Last session:** 2026-07-21, closed via `End Today`. Confirmed "Caching & Scaling" mastered (closing out Stage E, 5/5), then built and got mastered confirmation on all four of Stage F's first topics in one long session: "WebSockets & Real-Time," "Background Jobs & Message Queues," "Microservices vs Monolith," and "Beyond REST: GraphQL & tRPC Awareness."

## What happened this session, in order

1. **Confirmed "Caching & Scaling" mastered** — the one item carried over from last session. **Stage E is now fully complete (5/5)**.
2. **Built "WebSockets & Real-Time" (Stage F topic 1)** — 4 real hands-on sections: polling (measured 10 real requests, only 4 real price changes, 6 wasted), Server-Sent Events (one real HTTP connection, 5 real server-pushed events), raw WebSockets (a real `ws` server needing a raw `node:http` server underneath Express, real two-way "welcome"/"ping"/"pong" exchange), and Socket.io rooms (3 real clients, 2 rooms, real proof a broadcast only reaches sockets in the same room). Plus an Interview Angle recap. Verified via direct execution, a full production build (all demos executing live during prerendering), and a 3x concurrent-load check.
3. **Built "Background Jobs & Message Queues" (Stage F topic 2)** — real BullMQ against the machine's existing local Redis (no new infra decision needed). Sections: why background jobs (measured ~2000x faster response from enqueueing vs. blocking, on the identical task), real producer/worker split across two genuinely separate OS processes (the worker spawned and proven via its own separate stdout), real retries with backoff (a job engineered to fail its first 2 attempts, real `attemptsMade`/`failedReason` tracked live), and BullMQ-vs-RabbitMQ-vs-Kafka kept conceptual per the roadmap's own scoping (no new heavy infra installed). All PID-scoped to avoid the same concurrent-Server-Component-render races found in earlier sessions.
4. **Built "Microservices vs Monolith" (Stage F topic 3)** — the standout section: a real monolith (~0.01ms in-process "join") vs. the same operation split across two real separate Express services (~26-40ms real measured network call), then a section that actually `SIGKILL`s one real service process mid-demo and proves the other keeps answering its own endpoints (real 200) while only the genuinely dependent endpoint degrades to a deliberate, caught 503 — real fault isolation, not asserted. Distributed-complexity costs (network failure modes, cross-service consistency, tracing) kept conceptual, matching the BullMQ/RabbitMQ precedent.
5. **Built "Beyond REST: GraphQL & tRPC Awareness" (Stage F topic 4)** — scoped to "awareness-level" per the roadmap, but still fully real code, no fake stand-ins: REST over/under-fetching measured directly (2 real requests, 3 real unused fields), a real `graphql-yoga` server solving both in 1 real request (verified a resolver only runs when a query actually asks for that field), and a real tRPC server/client pair where the server exports only a TYPE (`AppRouter`, confirmed genuinely erased at runtime by Node's native type-stripping) and a deliberately wrong client call is rejected by `tsc` before any request is ever sent — reused `TypedBackendSetup`'s existing isolated-broken-file + separate-`tsconfig` pattern rather than inventing a new one. First new TypeScript-in-`examples/` mini-project since `TypedBackendSetup`; needed `@types/node` + an explicit `types: ["node"]` tsconfig entry to typecheck cleanly (now documented).
6. **Found and documented 4 new real environment/tooling gotchas** in `build-conventions.md` (see below) and **sharpened the standing `demo.js` rule**: the user declined an offered "upgrade" to `GraphQlYogaBasics/demo.js` (switching a hardcoded query literal to real GraphQL variables) with a direct restatement of the rule — `demo.js` only ever calls the real API and prints the output, it doesn't accumulate other logic even when well-intentioned. Saved as an extension to the existing `feedback_demo_js_calls_api_not_orm` memory.
7. Every topic this session verified the same way: real direct execution of each mini-project before wiring it into the app, a full clean `rm -rf .next && npm run build` (all demos executing live during static prerendering, not faked), and a 3x concurrent-`curl` stress check on the new page — worth calling out specifically for "Background Jobs" and "Microservices vs Monolith," both of which spawn real child processes and/or touch shared local Redis, exactly the kind of thing that caused real races in earlier sessions.

## Real infrastructure

No new *external* infra this session — reused the machine's existing local Redis (via brew) for BullMQ, and no database/blob-storage dependency for any of the 4 new topics. New real *npm dependencies*, all verified against their real current published versions after one guessed-version failure (`socket.io`/`socket.io-client` ETARGET, fixed): `ws`, `socket.io` + `socket.io-client`, `bullmq`, `graphql` + `graphql-yoga`, `@trpc/server` + `@trpc/client`, `@types/node` (dev, for the new TS example).

Highest fixed port assigned across the whole project: **4117** (`BeyondRestGraphqlTrpc/TrpcEndToEndTypes`). Full block this session: 4103-4106 (WebSockets), 4107-4109 (Background Jobs), 4110-4114 (Microservices vs Monolith — 4110 monolith, 4111/4112 split services, 4113/4114 independent-failure services), 4115-4117 (Beyond REST).

## Next unbuilt / in progress

**Stage F is now 4/6 mastered.** Next unbuilt topics, in roadmap order:
1. **"Backend System Design Drills"** — live-coding a REST API under time pressure, system design interview practice (pagination/idempotency/rate-limiting as discussion anchors, all already built earlier), plus framing-lens-only content: CAP theorem (mapped onto the real Postgres-vs-Mongo tradeoffs from Stage C), sharding/consistent hashing, and the classic latency-numbers table — all interview talking points, not hands-on builds, per the roadmap's own explicit scoping.
2. **"Working With a Frontend Team"** — API contracts, OpenAPI/Swagger, mocking a backend for a frontend dev, using the existing frontend background as a real interview differentiator.

Once both are built and confirmed, **Stage F (and the entire curriculum) is complete.**

## Open gaps / weak spots

- Carried forward, unresolved from earlier sessions: REST Conventions, Auth Patterns, and CORS's `PostmanCheck`s still predate the exhaustive-steps/request-response-separation fixes from 2026-07-18. Still just flagged, not actioned.

## Momentum notes carried forward

1. **Before writing ANY new dependency version into a `package.json`, run `npm view <package> version` first** — a guessed version number (`socket.io@^4.9.0`) failed installation outright this session. Cheaper to check once than to hit a real install failure mid-build.
2. **A new TypeScript-in-`examples/` mini-project needs `@types/node` (devDependency) AND `"types": ["node"]` in its own `tsconfig.json`** the moment it references `process`, `process.env`, or any `node:*` builtin — confirmed this gap exists even though the original `TypedBackendSetup` example never hit it (it never referenced Node builtins directly).
3. **To capture a real compiler (or any CLI tool's) error output when a non-zero exit is the EXPECTED correct outcome**, wrap `execSync` in try/catch and read `error.stdout` inside the catch block — confirmed it holds the exact real captured text, not just an opaque failure.
4. **The `demo.js`-only-calls-the-API rule is broader than "no ORM imports"** — it means `demo.js` stays a thin call-and-print layer, full stop. Any concept worth teaching (like GraphQL variables) becomes real page content, never a quiet enhancement folded into the existing demo script. Confirmed directly this session when the user declined exactly this kind of offered "improvement."
5. Momentum notes from 2026-07-21's earlier session (proactive port-0/PID-scoping/matchAll-timeout discipline on any demo touching a shared resource or spawning a process, honest "try it yourself" fallback when a tool can't run in this sandbox, distinguishing "revert the feature" from "keep the independent bug fix") all still stand and were applied cleanly again this session — no new instances of any of those specific bugs recurred.
