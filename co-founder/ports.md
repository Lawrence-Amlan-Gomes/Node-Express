# Dev Server Port Tracking

Internal record so a new session knows what "my port" refers to, per the port-management rules in the root `CLAUDE.md`. Only the process/port recorded here as "mine" is safe for me to kill/restart on my own.

## Current state

- **Server running:** no — killed at `End Today`, 2026-07-21. Was port 3000 (`http://localhost:3000`), last PID 68004.
- **Start command:** `npm run dev` (from project root — starts the Next.js viewer app via Turbopack)
- **Started by me this session:** yes (2026-07-21, new session). Port 3000 was free at session start. Restarted four times (PID 67189 → 86774 → 10330 → 29421 → 68004) for clean `rm -rf .next && npm run build` verification passes — after "WebSockets & Real-Time" (Stage F topic 1), "Background Jobs & Message Queues" (Stage F topic 2), "Microservices vs Monolith" (Stage F topic 3), and "Beyond REST: GraphQL & tRPC Awareness" (Stage F topic 4). Killed cleanly at close; port 3000 confirmed clear.

## History

- 2026-07-13 — Project created, no server existed yet.
- 2026-07-13 — Built the Next.js viewer app shell (Dashboard/RoadMap/topic pages) + first real topic. Ran `npm install`, `npm run typecheck`, `npm run lint`, `npm run build` all clean, then started `npm run dev` on port 3000 (confirmed free beforehand). Verified live via `curl` — `/`, `/roadmap`, `/topics/what-is-nodejs`, and an unbuilt stub route all return 200.
- 2026-07-13 — Revised the `what-is-nodejs` topic content, then ran `rm -rf .next && npm run build` to verify the production build while the dev server (PID 38667) was still running — this corrupted its live cache (page started 500ing). Killed 38667 (mine, safe to kill) and restarted `npm run dev` clean on port 3000 (new PID 39346). Lesson: don't run `rm -rf .next`/`next build` against the same `.next` directory a live `npm run dev` is using — stop the dev server first, or use a separate check, next time.
- 2026-07-13 — `End Today`: killed PID 39346 (mine, started earlier this session) cleanly. Port 3000 confirmed clear before ending.
- 2026-07-13 — New session started via `@skillCoFounderMentor.md`. Confirmed port 3000 clear via `lsof`, started `npm run dev` fresh (PID 6170), verified 200 via `curl`.
- 2026-07-13 — Built Stage A topic 2, "Modules, npm & package.json." Killed PID 6170 (mine) to run `npm run build` cleanly per the confirmed cache-corruption gotcha, then restarted `npm run dev` fresh (new PID 6925). Verified 200 on the new topic route via `curl`.
- 2026-07-13 — User caught FilePointers on that page pointing at the ROOT project's package.json/package-lock.json instead of the example's own — fixed by adding a real dependency (picocolors) to the CjsEsmInterop example, running a real `npm install` there, and repointing FilePointers inside `examples/`. Killed PID 6925 (mine) to rebuild cleanly, restarted `npm run dev` fresh (new PID 11484). Verified via `curl`.
- 2026-07-13 — Built Stage A topic 3, "TypeScript with Node.js" (real typed example project with its own tsconfig/package.json, 3 live runners). Also fixed a real gap this surfaced: root `tsconfig.json`/`eslint.config.mjs` weren't excluding `examples/`, so the new example's real `.ts` files were leaking into the root app's own typecheck/lint — added `examples` to both. Killed PID 11484 (mine) to rebuild cleanly, restarted `npm run dev` fresh (new PID 13499). Verified via `curl`.
- 2026-07-13 — Added the app favicon (`src/app/icon.svg`, same hexagon+"N" mark as the sidebar's NodeLogo) and a sidebar text-wrapping fix — both required a rebuild to verify (`/icon.svg` as a real static route). Killed PID 13499 (mine), restarted fresh (new PID 17374).
- 2026-07-13 — Built Stage A topic 4, "Core Modules" (real tour script covering path/fs/Buffer/structuredClone/events/process/Headers/FormData/fetch, including a real local http server the script starts itself). Killed PID 17374 (mine) to rebuild cleanly, restarted `npm run dev` fresh (new PID 22874). Verified via `curl`.
- 2026-07-14 — Built Stage A topic 5 (final Stage A topic), "Async Patterns on a Server" — a real callback/promise/async-await file-read demo, plus a measured blocking-vs-non-blocking demo on a real local http server. Found and fixed a genuine flaky-race in the blocking demo along the way (see build-conventions.md's "same-process blocking-demo gotcha"). Killed PID 22874 (mine) to rebuild cleanly, restarted `npm run dev` fresh (new PID 24177). Verified via `curl`.
- 2026-07-14 — Stage A fully mastered; built Stage B topic 1, "App & Router Basics (Express 5)" — a real installed Express 5.2.1 app (params/query/body/router/wildcard routes), confirmed two genuine Express 5 behaviors directly (bare '*' wildcard now throws; named wildcard captures are arrays, not strings). Also found and fixed a real ESM main-module-detection bug in server.js (see build-conventions.md's "ESM run directly vs imported" note). Killed PID 24177 (mine) to rebuild cleanly, restarted `npm run dev` fresh (new PID 25968). Verified via `curl`.
- 2026-07-14 — User caught two more real gaps on that same topic: server.js needed generous plain-English comments (not the terse "why only" style), and Section 1 had no live demo at all. Fixed both (rewrote server.js comments, added root-route-demo.js + its runner). Then caught a THIRD instance of the same underlying issue: server.close() was used in demo scripts across three already-shipped topics with no in-UI explanation anywhere — fixed by adding the full explanation to Core Modules (first place it appears in build order) and short back-references on Async Patterns and Express App Routing. Killed PID 25968/27553 (mine) across two rebuild passes, restarted `npm run dev` fresh (final PID 29536). Verified via `curl`.
- 2026-07-14 — User asked whether all real APIs go in server.js — surfaced a real gap: the page claimed "real apps split routes across files" but the example never actually did that (Router was defined inline in server.js). Fixed by moving apiRouter into a real separate file, api-routes.js, imported by server.js — the claim is now proven, not just asserted. Killed PID 29536 (mine) to rebuild cleanly, restarted `npm run dev` fresh (new PID 30154). Verified via `curl`.
- 2026-07-14 — Built Stage B topic 2, "The Middleware Pipeline" — 3 focused real demos (registration-order proof, an auth-check that only works in the correct position, and a real timeout-race proving a forgotten next() hangs a request forever). Killed PID 30154 (mine) to rebuild cleanly, restarted `npm run dev` fresh (new PID 30967). Verified via `curl`.
- 2026-07-14 — User asked whether supporting tools (Postman, Thunder Client, etc.) were covered — a real gap, added as a new Stage B topic, "API Testing Tools: curl, Postman & Thunder Client," inserted right after Middleware Pipeline. Built a real todos API on a fixed port (4010, unlike other topics' ephemeral-port pattern, since curl/Postman/.http files all need a stable URL), a real importable Postman collection, a real .http request file, and a curl-demo.js that spawns the server as a real separate process and runs real curl commands against it. Killed PID 30967 (mine) to rebuild cleanly, restarted `npm run dev` fresh (new PID 37300). Verified via `curl`.
- 2026-07-14 — User caught a real structural mistake across all 3 built Stage B topics: sections covering genuinely unrelated concepts were sharing one server.js/example project. Fixed by splitting every section into its own separate mini-project — Middleware Pipeline (OrderOfExecution/AuthOrderMatters/ForgottenNext), App & Router Basics (CreatingTheApp/RoutesParamsQueryBody/NamedWildcards — the wildcard section also got a real live demo it never had before), and API Testing Tools (CurlWorkflow/PostmanCollection/HttpRequestFile — the latter two also gained real live demos). Deleted the old shared folders (PipelineOrder, BasicExpressApp, ExploringTheApi). Recorded as a standing rule in build-conventions.md and memory. Killed PID 37300 (mine) to rebuild cleanly, restarted `npm run dev` fresh (new PID 41035). Verified via `curl` across all three topics.
- 2026-07-14 — User wanted a visual sense of the real Postman UI instead of terminal-style JSON output for the Postman Collection section. Built `src/components/PostmanMockUI.tsx` — a static, orange-branded illustration of Postman's real layout (sidebar of saved requests, URL/Send bar, response panel), populated entirely with the same real, already-verified response data from that section's live demo (kept alongside it, not replaced, so the mockup's data stays backed by real execution). Killed PID 41035 (mine) to rebuild cleanly, restarted `npm run dev` fresh (new PID 44379). Verified via `curl`.
- 2026-07-14 — `End Today`: killed PID 44379 (mine, started earlier this session) cleanly. Port 3000 confirmed clear before ending.
- 2026-07-15 — New session started via `@skillCoFounderMentor.md`. Port 3000 was occupied by a DIFFERENT project's dev server (next-server out of `/Users/lawrencealangomes/Documents/English`, PID 6852/6851, cwd confirmed via `lsof` — not something this session started, so left untouched per the "never kill a process you didn't start" rule). Started this project on port 3001 instead (`npm run dev -- -p 3001`, PID 10561). Verified 200 via `curl`.
- 2026-07-15 — Long session: built Error Handling in Express and Project Structure & Config (completing Stage B), then SQL vs NoSQL (starting Stage C). Restarted the dev server 5 times total across the session for build-verification passes (PIDs 10561 → 12925 → 14461 → 15850 → 18560 → 24638), each time killing the previous one (mine) first per the cache-corruption gotcha. Killed the final one (24638) cleanly at `End Today`.
- 2026-07-15 — New session started via `@skillCoFounderMentor.md`. Both port 3000 (`/Users/lawrencealangomes/Documents/English`) and 3001 (`/Users/lawrencealangomes/Documents/Next Js`) were occupied by other unrelated projects' dev servers — neither started by this session, so left untouched. Started this project on port 3002 instead (`npm run dev -- -p 3002`, PID 12088). Verified 200 via `curl` on the new `orm-query-builder` topic page.
- 2026-07-16 — Long session (Stage C + Stage D fully built and mastered, 8 topics total) continued using the same PID 12088 on port 3002 throughout — no restarts needed this session. Killed cleanly at `End Today`.
- 2026-07-16 — New session continued via `@skillCoFounderMentor.md`. Port 3000 was free this time. Started fresh (PID 54198), restarted once after a production `npm run build` verification pass (new PID 57106, then final PID 58483 after fixing the Jest-stderr execSync bug — see build-conventions.md). Building Stage E topic 1, "Testing an Express App."
- 2026-07-17 — New session via `@skillCoFounderMentor.md`. Port 3000 free, started fresh (PID 31067). This became a long "detail-rebuild pass" session (see `current-state.md`) — restarted the dev server 9 times total (PID 31067 → 37644 → 39287 → 40113 → 41069 → 42646 → 43546 → 46978 → 48172 → 53470), each restart running a clean `npm run build` verification pass right after rebuilding one topic (What is Node.js → Modules/npm → TypeScript with Node → Core Modules → Async Patterns → API Testing Tools → App & Router Basics, plus a mid-pass PostmanCheck component refinement). Killed the final PID (53470) cleanly at `End Today`.
- 2026-07-18 — New session via `@skillCoFounderMentor.md`. Port 3000 occupied by a different project (`English`), started on 3001 instead. Long session completing the detail-rebuild pass (Stage D full rebuild + Stage E topic 1 rebuild/mastered) and building Stage E topic 2 fresh. Killed cleanly at `End Today`.
- 2026-07-19 — New session via `@skillCoFounderMentor.md`. Port 3000 occupied by a different, unrelated project (`next-server`, cwd `/Users/lawrencealangomes/Documents/MySelf`) — left untouched. Started this project on port 3001 (PID 8973), verified `✓ Ready` in the startup log.
- 2026-07-20 — New session via `@skillCoFounderMentor.md`. Port 3000 was free. Started fresh (PID 26883), verified `✓ Ready` in the startup log. Built Stage E topic 3, "Containerization & Deployment" — restarted twice for clean build-verification passes (PID 26883 → 44788 → 50995), also installed Docker Desktop (new real infra) and deployed a real app to the user's Coolify instance during the session. Killed cleanly at `End Today`.
- 2026-07-21 — Long session (see current-state.md's own 2026-07-21 entry): built Debugging & Memory Profiling and Caching & Scaling, plus a Remotion pilot built then fully reverted. Killed cleanly at `End Today`.
- 2026-07-21 — New session via `@skillCoFounderMentor.md`. Port 3000 was free. Started fresh (PID 67189), verified `✓ Ready in 325ms` in the startup log. Long session (see current-state.md): built and mastered "WebSockets & Real-Time," "Background Jobs & Message Queues," "Microservices vs Monolith," and "Beyond REST: GraphQL & tRPC Awareness" — restarted 4 times for clean build-verification passes (PID 67189 → 86774 → 10330 → 29421 → 68004). Killed cleanly at `End Today`.
